import React from "react";
import { Table } from "../../components";
import { Link } from "react-router-dom";
import Button from "@mui/material/Button";

const CreateEvents = () => {
	return (
		<div>
			<div className="event-create-text">Create Events</div>
		</div>
	);
};

export default CreateEvents;
